stocks:
  outputs:
    dev:
      type: duckdb
      schema: main
      path: 'md:stocks_dev?$MOTHERDUCK_TOKEN'
      threads: 4
      extensions:
        - httpfs

    prod:
      type: duckdb
      schema: main
      path: 'md:stocks'
      threads: 1

    local:
      type: duckdb
      schema: main
      path: 'stocks_local.duckdb'
      threads: 4

  target: dev

